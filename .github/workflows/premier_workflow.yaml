name: Toto
on: [push]
jobs:
  MonPremierJob:
    runs-on: ubuntu-latest
    steps: 
      - name: Recup du repo
        uses: actions/checkout@v3
      - name: test 1
        run: exit 1
      - if: ${{ always() }}
        name: echo "test 2"
      - name: Envoi mail
        if: ${{ failure() }}
        run: echo "Envoi d'un mail si le test échoue"