name: Toto
on: [push]
jobs:
  MonPremierJob:
    runs-on: ubuntu-latest
    steps: 
      - name: Recup du repo
        uses: actions/checkout@v3
      - name: afficher le contenu de mon job
        run: ls
      - name: Envoi mail
        if: ${{ failure() }}
        run: echo "Envoi d'un mail si le test échoue"